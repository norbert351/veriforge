node_modules/
dist/
.next/
.env
.env.local
*.tsbuildinfo
# Commit the addresses: the API reads this file at runtime and a Render clone
# has no other way to learn the deployed registries (public testnet data).
.cache/
.keys/
apps/api/uploads/